---
# Timeline entry — use scalar `type` (see CONTEXT.md), not list-style `type: - …`
type: event
in_world_start:
in_world_end:
precision: approximate
in_world_label:
status: draft
sources:
related_events:
related_world:
# Newspapers only:
publication:
edition:
---

# Title

Body. For `type: newspaper`, write in-fiction press style. For `type: backstory`, link `related_world` and downstream `related_events`.
